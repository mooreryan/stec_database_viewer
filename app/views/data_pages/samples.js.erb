// dynamic changes ie use of render etc need to be in the views folder ot in the assests folder
// who knows why!?

$('#table-container').html("<%= escape_javascript(render :partial => 'layouts/samples_table')%>");
