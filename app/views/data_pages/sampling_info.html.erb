<% provide(:title, "Sampling Info") %>

<h1>View</h1>
<h2>Sampling Info</h2>

<%= render 'layouts/sample_nav_bar' %>

<div id="table-container" class="table-container">
  <table class="scroll-table sortable">
    <thead class="fixed-header">
      <tr class="header-row">
	<th>Sample date</th>
	<th>Pen ID</th>
	<th># head</th>
	<th>Oberservations</th>
      </tr>
    </thead>
    <tbody class="scroll-content">
      <% SamplingInfo.all.each do |info| %>
	<tr>
	  <td><%= info.sample_date %></td>
	  <td><%= info.pen_id  %></td>
	  <td><%= info.head_per_pen %></td>
	  <td><%= info.observations %></td>
	</tr>
	<% end %>
    </tbody>
  </table>
</div>

